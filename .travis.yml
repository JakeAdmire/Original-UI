language: node_js
node_js:
  - 9
  - 8
script:
  - npm ci
  - npm install --global rollup
  - npm test -- --collectCoverage=true
  - npm run coveralls
deploy:
  provider: npm
  email: NPM_EMAIL
  api_key: NPM_TOKEN
  on:
    all_branches: true